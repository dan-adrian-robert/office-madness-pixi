
export class ScreenSystem {
    currentScreen: any;
    gameState: any;

    constructor(
        currentScreen: any,
        gameState: any,
    ) {
        this.currentScreen = currentScreen;
        this.gameState = gameState;
    }
}